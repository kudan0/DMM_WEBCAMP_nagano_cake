<!--ジャンル検索(テンプレをつくる)-->

<h3>商品一覧</h3>
<div>

  <% @items.each do |item| %>
  <%= link_to item_path(item.id) do %>
    <%= attachment_image_tag item, :image, size: "150x100" %>
    <%= item.name %>
    ￥<%= (item.price * 1.1).round %>
  <% end %>
  <% end %>
</div>

<%= paginate @items %>
